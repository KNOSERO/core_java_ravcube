ravcube:
  testcontainers:
    kafka:
      enabled: false

ravcube:
  kafka:
    listener:
      auto-startup: true
    consumer:
      group-id: event-core-kafka-test
      auto-offset-reset: earliest
      trusted-packages: com.ravcube.lib
  testcontainers:
    kafka:
      enabled: true